// Barrel file - re-export UI primitives so `@/components/ui` works everywhere.

/* Buttons */
export { Button } from "./button"

/* Cards */
export {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
} from "./card"

/* Badges */
export { Badge } from "./badge"

/* Tables */
export {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "./table"

/* (Add more exports below as your codebase grows) */
